import { SectionCard } from '@/components/ui/section-card'

export default function InboxPage() {
  return <SectionCard title="Inbox">Unified message center (client + internal threads) goes here.</SectionCard>
}

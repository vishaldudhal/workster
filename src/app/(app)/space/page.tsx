import { SectionCard } from '@/components/ui/section-card'

export default function SpacePage() {
  return <SectionCard title="Space">Project-wise file manager (folders, permissions, previews) module shell.</SectionCard>
}

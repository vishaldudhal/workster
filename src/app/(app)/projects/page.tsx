import { SectionCard } from '@/components/ui/section-card'

export default function ProjectsPage() {
  return <SectionCard title="Projects">Kanban, task board, templates, and time tracking module shell.</SectionCard>
}
